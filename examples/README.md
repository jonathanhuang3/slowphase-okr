# Example data

Synthetic upward OKR-like trace (~40 s at 90 Hz) for testing the GUI without patient data.

```bash
python make_example_data.py   # regenerate
slowphase-okr                 # load example_rotatedGaze.txt + example_gazeTime.txt
```
